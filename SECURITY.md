# Security policy

Report vulnerabilities privately through GitHub Security Advisories. Never place credentials, customer data, payment
details, production configuration, or exploit payloads containing real data in public issues.

Changes to authentication, authorization, billing, payment, public API routes, provider requests, or database migrations
require explicit security tests. The private inference service must be authenticated service-to-service and is never a
public trust boundary.
